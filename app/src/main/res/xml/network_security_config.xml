<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext (http://) is only permitted for local dev testing against the API
         running on your machine — 10.0.2.2 is the Android emulator's alias for your
         host machine's localhost. Everything else still requires HTTPS. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
